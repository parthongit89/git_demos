**Git setup commands**
```
git config --global user.name "username"
git config --global user.email "username.email.com"
git config --list

```
** Setup actual  :  **
```
git config --global user.name "parthongit89"
git config --global user.email "sonavaneparthgit@gmail.com"
git config --list
```

** cloning repos  **
```

 git clone  https://github.com/parthongit89/health_check.git

```